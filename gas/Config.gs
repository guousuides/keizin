/**
 * ============================================================
 *  夏合宿 競馬企画 — 共通設定
 * ============================================================
 *  ここはシートの「骨格」を決める定数です。
 *  当日いじる係数（複勝係数 a・持ち点・オッズ下限など）は
 *  スプレッドシートの「設定」シートB列で変えてください。
 *  この Config.gs を触るのは
 *    ・出走人数を6人から変える
 *    ・チーム数を10から変える
 *  ときだけです。
 * ============================================================
 */

const CFG = {
  /** 出走する代表者の人数（＝馬の頭数）。画像の A_i の i の個数。 */
  HORSE_COUNT: 6,

  /** 参加チーム数。 */
  TEAM_COUNT: 10,

  /**
   * 「精算」シートにあらかじめ数式を敷いておく行数。
   * ＝1レースで受け付けられる購入の最大本数。
   * 10チームが各10点買っても100本なので300で充分。
   */
  BET_ROWS: 300,

  /** シート名。変えると全数式に影響するので原則そのまま。 */
  SHEETS: {
    SETTING: '設定',
    HORSE:   '出走馬',
    TEAM:    'チーム',
    LOG:     '購入ログ',
    ODDS:    'オッズ',
    RESULT:  '結果',
    PAYOUT:  '精算',
    DOC:     '解説',
  },

  /** 券種。Webアプリの選択肢もこの並び順で出ます。 */
  TICKETS: ['単勝', '複勝', '三連単', '三連複'],

  /** 各券種で何頭指名させるか（Webアプリの入力欄の出し分けに使う）。 */
  PICKS: { '単勝': 1, '複勝': 1, '三連単': 3, '三連複': 3 },

  /** データ行の開始行（1〜3行目は見出し・説明に使う）。 */
  FIRST_ROW: 4,
};

/** 設定シートの各パラメータが何行目にあるか（名前付き範囲の定義に使う）。 */
const SETTING_ROWS = {
  複勝係数:   4,
  控除率:     5,
  オッズ下限: 6,
  オッズ上限: 7,
  三連単係数: 8,
  三連複係数: 9,
  初期持ち点: 10,
  受付状態:   11,
  丸め単位:   12,
  三連系方式: 13,
  レース名:   14,
};

/** 三連単・三連複のオッズ計算方式の選択肢。 */
const TRIFECTA_MODES = {
  SIMPLE:   '単純積',        // 画像の式そのまま： O_i × O_j × O_k
  HARVILLE: 'Harville補正',  // 期待払戻が100%になるよう補正した版
};
